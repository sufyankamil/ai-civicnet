enum HelpCategory {
  errands,
  techSupport,
  emergency,
  education,
  transport,
  household,
  other,
  health
}

enum UrgencyLevel {
  low,
  medium,
  high,
  critical
}

enum RequestStatusEnum {
  open,
  inProgress,
  completed,
  closed
}
